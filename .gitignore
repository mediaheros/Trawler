node_modules/
dist/
src-tauri/target/
src-tauri/gen/
*.local
.DS_Store
# Release binaries stay out of Git, but the updater manifest is source and
# must be reviewed so one platform cannot silently overwrite the others.
docs/dl/*
!docs/dl/latest.json
dev.cmd
.claude/

# private working notes for AI-assisted sessions
CLAUDE.md

# the updater signing key must never enter this repo, under any name
*.key

# local screenshot-rig output
scripts/.out/
